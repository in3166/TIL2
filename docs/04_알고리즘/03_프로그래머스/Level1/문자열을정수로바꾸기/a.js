function solution(s) {
    var answer = 0;
    var s1 = "-1";
    console.log(parseInt(s1));
    return answer;
}

console.log(solution("1234"));

// 다른 풀이: 사칙연산 되면서 문자가 자동으로 파싱
function strToInt(str) {
    return str / 1
    return +str;
}